apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-manager.fullname" . }}
  labels:
    {{- include "todo-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todo-manager.selectorLabels" . | nindent 4 }}
