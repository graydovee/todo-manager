# Backend
# frontend_dist is a build artifact, but keep a .gitkeep placeholder so the
# //go:embed in static.go compiles on a fresh checkout (before any build).
backend/static/frontend_dist/*
!backend/static/frontend_dist/.gitkeep
backend/todolist.db
*.db

# Frontend
frontend/node_modules/
frontend/dist/

# Desktop (Tauri)
desktop/node_modules/
desktop/dist/
desktop/src-tauri/target/

bin/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Config (keep example, ignore local)
config.yaml

# Build
*.tsbuildinfo
todolist

!charts/todolist
charts/todolist/charts/*.tgz

CLAUDE.md
.kiro
.zcode/